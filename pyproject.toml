# Copyright © 2026 Hanafi Mohd Radi. All rights reserved.

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "faceset-curator"
version = "0.2.0"
description = "FaceSort by HMR Studio — local face-image sorting and selection"
authors = [{ name = "Hanafi Mohd Radi" }]
license = { text = "Copyright © 2026 Hanafi Mohd Radi. All rights reserved." }
classifiers = ["License :: Other/Proprietary License"]
requires-python = ">=3.11"
dependencies = ["Pillow>=10.0", "psutil>=6.0"]

[project.optional-dependencies]
gpu = [
  "numpy>=2.0",
  "opencv-python-headless>=4.10",
  "onnx>=1.16",
  "onnxruntime-gpu[cuda,cudnn]>=1.21,<1.27",
  "insightface>=0.7.3",
]
test = ["pytest>=8.0"]

[project.scripts]
facesort = "faceset_curator.cli:main"
facesort-gui = "faceset_curator.gui:main"
fsc = "faceset_curator.cli:main"
fsc-gui = "faceset_curator.gui:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
faceset_curator = ["assets/*.ico"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
